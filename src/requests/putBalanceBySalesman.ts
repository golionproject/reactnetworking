import { AxiosError } from "axios";

async function putBalanceBySalesman(
  // @ts-ignore
  balance: number
): Promise<void | AxiosError> {
  return Promise.resolve();
}

export default putBalanceBySalesman;
